---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: akahu-actual
  namespace: actual
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  dependsOn:
    - name: actual
      namespace: actual
  values:
    controllers:
      akahu-actual:
        type: cronjob
        cronjob:
          schedule: 49 * * * *
        containers:
          tag:
            image:
              repository: ghcr.io/scottmckendry/akahu-actual
              tag: v0.9.0@sha256:2be76e9936f255950d2b0fa2ab0b321e64622d194a1d22fb16283ab84e00fb2b
            envFrom:
              - secretRef:
                  name: akahu-actual-secret
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
    persistence:
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
            readOnly: false
