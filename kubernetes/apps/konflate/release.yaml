---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: konflate
  namespace: konflate
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: konflate
    namespace: flux-system
  values:
    config:
      repo: github://scottmckendry/axis
      clusterPath: ./kubernetes
      refreshInterval: 30m
      maxDiffConcurrency: 1
      helmTemplateCacheMb: "128"
      logLevel: info
      logFormat: json
      mergeCommand: gh pr merge {{ .Number }} --repo {{ .Repo }} --rebase --delete-branch

    secret:
      existingSecret: konflate-webhook

    persistence:
      enabled: true
      storageClass: iscsi
      size: 5Gi

    ingress:
      enabled: false

    resources:
      limits:
        memory: 2Gi
      requests:
        cpu: 50m
        memory: 512Mi
